{
  "name": "eth-avax-3",
  "version": "1.0.0",
  "description": "This Solidity smart contract implements a basic ERC20 token with additional functionalities using OpenZeppelin libraries. The contract allows for minting new tokens by the owner, burning tokens by any holder, and transfers following the ERC20 standard.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.5"
  }
}
